# Docs
- `01-stage0-testkit.md` — the four-week no-code field test (route, WhatsApp template, TikTok format, buyer outreach, decision gates).
- `stage0-sku-list.csv` — the 53 products with brand, category, compare unit and an empty EAN column (committed). `stage0-collection-sheet.csv` (53 products × 8 chains, one row per shelf check) is generated with `python3 scripts/make_stage0_sheets.py` from the repo root (Google Sheets → File → Import).
- The full second-opinion & scoping report (market, competitors, data/legal, options, scores) was delivered separately as `price-truth-app-second-opinion.md`; drop it here as `00-second-opinion-and-scoping.md` when you clone the repo.
